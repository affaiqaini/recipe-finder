/* Loading component displayed while recipe data is being fetched
 * Used in recipe details page and search results
*/
export const Loading = () => (
    <div className="recipe-container">Loading...</div>
  );